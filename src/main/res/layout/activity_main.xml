<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.tripint.intersightplus.MainActivity">

    <FrameLayout
        android:id="@+id/main_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="8"
        ></FrameLayout>

    <RadioGroup
        android:id="@+id/main_tabBar"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        >
        <RadioButton
            android:id="@+id/main_relation"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:button="@null"
            android:layout_height="wrap_content"
            android:text="首页"
            android:textColor="@color/selector_textcolor"
            android:textSize="12sp"
            android:gravity="center"
            android:drawableTop="@drawable/selector_relation"
            />


        <RadioButton
            android:id="@+id/main_discover"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:button="@null"
            android:layout_height="wrap_content"
            android:text="发现"
            android:textColor="@color/selector_textcolor"
            android:textSize="12sp"
            android:gravity="center"
            android:drawableTop="@drawable/selector_discover"
            />
        <RadioButton
            android:id="@+id/main_mine"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:button="@null"
            android:layout_height="wrap_content"
            android:text="我的"
            android:textColor="@color/selector_textcolor"
            android:textSize="12sp"
            android:gravity="center"
            android:drawableTop="@drawable/selector_person"
            />
        <RadioButton
            android:id="@+id/main_more"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:button="@null"
            android:layout_height="wrap_content"
            android:text="更多"
            android:textColor="@color/selector_textcolor"
            android:textSize="12sp"
            android:gravity="center"
            android:drawableTop="@drawable/selactor_strategy"
            />
    </RadioGroup>
</LinearLayout>
